import { ToolType } from '../models/ToolType';

export const CALCULATOR: ToolType = {
  name: 'calculator',
};
export const IMAGE: ToolType = {
  name: 'image',
};

export const toolTypes: Array<ToolType> = [CALCULATOR, IMAGE];
