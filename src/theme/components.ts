const headingTheme = {
  baseStyle: {
    fontWeight: 500,
  },
};

const buttonTheme = {
  baseStyle: {
    textTransform: 'uppercase',
  },
};

const components = { Button: buttonTheme, Heading: headingTheme };

export default components;
